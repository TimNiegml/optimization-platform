# All permissive (MIT / BSD / Apache-2.0) — safe for closed-source / redistribution.
pydantic>=2.0      # MIT     — VOCS / config schema + validation
numpy              # BSD-3   — numerics
scipy              # BSD-3   — line search / optimization primitives
lmfit              # BSD-3   — quadratic / gaussian curve fitting (R², bounds)
asteval            # MIT     — whitelisted safe evaluation of if/loop conditions
pyyaml             # MIT     — declarative pipeline config
streamlit          # Apache-2.0 — MVP web UI
pandas             # BSD-3   — history / plotting
optuna             # MIT     — Bayesian / TPE generator (lazy-imported wrapper)
fastapi            # MIT     — backend service (catalog + run endpoints)
uvicorn            # BSD-3   — ASGI server for the API
httpx              # BSD-3   — used by the API tests (TestClient)
mcp>=1.2,<2        # MIT     — L2 MCP server (expose the platform to any MCP agent)
                   #           2.0 removed `mcp.server.fastmcp`; upper bound keeps
                   #           optplat.mcp_server (and therefore optplat.api) importable.
sse-starlette      # BSD-3   — SSE stream for live canvas refresh (ships with mcp)
# sqlite3 is stdlib (archive/resume/rollback); PyVISA/PyMeasure (MIT) optional,
# only needed for real instruments — the simulated stage/meter need nothing.
# The drag-drop canvas (web/index.html) is dependency-free vanilla JS — no CDN.
